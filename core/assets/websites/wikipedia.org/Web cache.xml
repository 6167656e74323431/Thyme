<:import macros/html.xml />
<:h1>Web cache</:h1>
<:p>A web cache (or "HTTP cache") is an information technology for the temporary storage (caching) of Web documents, such as web pages, images, and other types of Web multimedia, to reduce server lag. A web cache system stores copies of documents passing through it; subsequent requests may be satisfied from the cache if certain conditions are met. A web cache system can refer either to an appliance, or to a computer program.</:p>
<:p>Web caches can be used in various systems (as viewed from direction of delivery of web content):</:p>
<:p>A forward cache is a cache outside the web server's network, e.g. on the client computer, in an ISP or within a corporate network. A network-aware forward cache is just like a forward cache but only caches heavily accessed items. A client, such as a web browser, can also store web content for reuse. For example, if the back button is pressed, the local cached version of a page may be displayed instead of a new request being sent to the web server. A web proxy sitting between the client and the server can evaluate HTTP headers and choose whether to store web content.</:p>
<:p>A reverse cache sits in front of one or more web servers and web applications, accelerating requests from the Internet, reducing peak web server load. A content delivery network (CDN) can retain copies of web content at various points throughout a network. A search engine may also cache a website; it provides a way of retrieving information from websites that have recently gone down or a way of retrieving data more quickly than by clicking the direct link. Google, for instance, does so. Links to cached contents may be found in Google search results.</:p>
<:p>HTTP defines three basic mechanisms for controlling caches: freshness, validation, and invalidation.</:p>
<:p>- Freshness : allows a response to be used without re-checking it on the origin server, and can be controlled by both the server and the client. For example, the Expires response header gives a date when the document becomes stale, and the Cache-Control: max-age directive tells the cache how many seconds the response is fresh for.</:p>
<:p>- Validation : can be used to check whether a cached response is still good after it becomes stale. For example, if the response has a Last-Modified header, a cache can make a "conditional request" using the If-Modified-Since header to see if it has changed. The ETag (entity tag) mechanism also allows for both strong and weak validation.</:p>
<:p>- Invalidation : is usually a side effect of another request that passes through the cache. For example, if a URL associated with a cached response subsequently gets a POST, PUT or DELETE request, the cached response will be invalidated.</:p>
<:p>Many CDNs and manufacturers of network equipment have replaced this standard HTTP cache control with dynamic caching.</:p>
<:p>In 1998, the DMCA added rules to the United States Code (17 U.S.C. §: 512) that relinquishes system operators from copyright liability for the purposes of caching.</:p>
<:p>The following is a list of dedicated web caching server software:</:p>
<:p>- Content delivery network (CDN)</:p>
<:p>- Harvest project</:p>
<:p>- Proxy server</:p>
<:p>- Web accelerator</:p>
<:p>- Cache manifest in HTML5</:p>
<:p>- Ari Luotonen, "Web Proxy Servers" (Prentice Hall, 1997)</:p>
<:p>- Duane Wessels, "Web Caching" (O'Reilly and Associates, 2001).</:p>
<:p>- Michael Rabinovich and Oliver Spatschak, "Web Caching and Replication" (Addison Wesley, 2001).</:p>
<:p>- How to clear your browser's web cache</:p>
<:p>- HTTP Caching</:p>
<:p>- Web Caching, Web caching in general with some references to SQUID</:p>
<:p>- What is a Web Cache?</:p>